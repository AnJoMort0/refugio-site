# Roadmap

## Current phase
Prototype first.

## Current step
Homepage scaffold.

## Small changes to implement next
The eyebrow of the hero is still hard to read since the green is dark, so maybe we change that green into the yellow tone used for other elements for this specific eyebrow only.
The "Descobrir o espaço" and the "Ver guia local" button is too close to the text and not the same distance to the previous element like the other buttons. It should be consistent.
The titles were shrinked in the phone, they are still a little too big in desktop. Make them slightly smaller.
Add a very small space between <p>, I think about the O Alojamento section when I mention it.
Make sure the texts in index.html correspond one-to-one to the pt.json file, even if the page should always be based on the json files to write the text, the text hard-coded in index.html are the fallback in case of a problem, so it should perfectly reflect the Portuguese version. This includes the alt texts for images.
Can you add overall some animations, fade-ins and maybe mouvement when opening menus, make them very minimal, but beautiful, not invasive, luxurious, simple.
Update the README to correspond to the current progress of the website

## Small additions to not forget
Change default language based on browser defaults