# Roadmap

## Current phase
Prototype first.

## Current step
Homepage scaffold.

## Small changes to implement next
Change the opacity or darkness of the first background image cause the white text and mostly the eyebrow is hard to read
Turn the gallery and the reviews sections into a carrossel (6-8s auto scroll, manual scroll -> locks the auto-scroll until the page moves, see a little bit of the next item in the left and right to show it is a caroussel)
Make the links to other pages at the end of the paragraphs to look more like buttons, because right now it's not very clear. It shouldn't be as flashy as the Reservar button, should still be clear it's a button.
Since the galery is right after the O Alojamento, we could remove the image between those two sections and just add interior images into the carrossel
The Galeria section also should get a button to open the galeria.html page like the other sections
The Parceiros section also should get a button that will open the guia-local.html page (later in the correct section of that page)
The titles look a little too big on phone, I would make them ever so slightly smaller
The language dropdown should move to be all the way in the right in the header when the menu is written down and not the ☰
Turn the Parceiros section into a grid type thing so with 2 items per line, with an image and a subtitle for like "Restaurante bla bla bla - Especialidades Locais"

## Small additions to not forget
Change default language based on browser defaults